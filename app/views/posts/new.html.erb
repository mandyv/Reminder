<% provide(:page_title, @post.restaurant) %>
<% provide(:blog_title, "active") %>
<div class="jumbotron">
	<h1>New Reminder</h1>

	<%= render 'form', post: @post %>

	<%= link_to 'Back', posts_path %>
</div>